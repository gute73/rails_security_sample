<% if object.errors.any? %>

	<div class='error_messages'>
		<div class='alert alert-danger'>
			<%= pluralize(object.errors.count, "error") %> found.
		</div>
		<ul>
			<% object.errors.full_messages.each do |error| %>
				<li><%= error %></li>
			<% end %>
		</ul>
	</div>
<% end %>